{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Iniciar Docker Compose",
            "type": "shell",
            "command": "docker-compose up --build",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}